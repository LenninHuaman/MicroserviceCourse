server:
    port: 8001

spring:
    cloud:
        config:
            server:
                git:
                    default-label: Zipkin
                    uri: https://github.com/LenninHuaman/MicroserviceCourse
                    search-paths: config-data
    application:
        name: config-server

